/*
    math.PI - Traz o valor de PI
    math.abs(x) - Traz o valor absoluto de um número
    math.ceil(x) - Traz o número arredondado sempre para cima
    math.floor(x) - Traz o número arredondado sempre para baixo
    math.round() - Traz o número arredondado normal seguindo a regra
    math.sqrt(x) - Tira a raiz quadrada
    math.cbrt(x) - Tira a raiz cubica
    math.min([x1, x2, xN]) - (os parâmetros não são obrigatórios) Traz o maior número daqueles passados por parâmetros
    math.max([x1, x2, xN]) - (os parâmetros não são obrigatórios) Traz o menor número daqueles passados por parâmetros
        ex. uso:
        var numbers = [1, 2, 3, 4, 5, -10];
        Math.max.apply(Math, numbers);
    math.random() - Traz um número aleatório sempre maior que zero e menor que um
        * E se quiser de, por exemplo, zero a três:
        Math.random() * 3
        * E se quiser arredondado:
        Math.floor(Math.random() * 3) - Ira trazer do zero ao 2
        Math.ceil(Math.random() * 3) - Ira trazer do 1 ao 3
    

*/